---
- name: Creating links for sensor {{ config.sen }}
  file:
    src: '{{ pq_workspace }}/{{ config.sen }}/{{ config.sen }}.c'
    dest: '{{ pq_workspace }}/{{ item }}/libs/{{ config.sen }}.c'
    state: link
  with_items: '{{ config.sub }}'
